test('should add each digit together', function(){
  deepEqual(sumDigits(123), 6, 'added them together');
});

test('should add each digit together', function(){
  deepEqual(sumDigits(4891), 22, 'added them together');
});
